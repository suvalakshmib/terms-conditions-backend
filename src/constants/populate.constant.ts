const Populate = {
	user: {
		path: "user",
		select: "email username profile name",
	},
	from: {
		path: "from",
		select: "email username profile name",
	},
};

export default Populate;
